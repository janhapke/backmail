name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  test:
    runs-on: ubuntu-latest

    strategy:
      matrix:
        node-version: [24.x]

    steps:
      - uses: actions/checkout@v4

      - name: Use Node.js ${{ matrix.node-version }}
        uses: actions/setup-node@v4
        with:
          node-version: ${{ matrix.node-version }}
          cache: npm
      
      - name: pin npm version
        run: corepack enable && corepack prepare npm@11.6.2 --activate

      - name: Install dependencies
        run: npm ci --verbose

      - name: Type check
        run: npm run typecheck

      - name: Unit tests
        run: npm test

      # Integration tests require Docker with a Dovecot container.
      # Run locally with: npm run test:integration
