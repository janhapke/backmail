---
phase: 01-foundation
plan: 01
subsystem: infra
tags: [typescript, nodejs, esm, vitest, tsx, commander, npm]

# Dependency graph
requires: []
provides:
  - ESM package.json with pinned devDependencies (typescript 6.0.3, tsx 4.21.0, vitest 4.1.4, @types/node 25.6.0) and dependency (commander 14.0.3)
  - tsconfig.json with module=nodenext, moduleResolution=nodenext, strict=true, verbatimModuleSyntax=true
  - vitest.config.ts for unit tests only (testTimeout=10000)
  - vitest.integration.config.ts for integration tests (testTimeout=120000, hookTimeout=30000)
  - node_modules populated (53 packages, 0 vulnerabilities)
affects: [01-02, 01-03, 01-04, all-subsequent-phases]

# Tech tracking
tech-stack:
  added: [typescript@6.0.3, tsx@4.21.0, vitest@4.1.4, "@types/node@25.6.0", commander@14.0.3]
  patterns: [nodenext-esm-module-resolution, unit-integration-test-separation, pinned-exact-versions]

key-files:
  created:
    - package.json
    - tsconfig.json
    - vitest.config.ts
    - vitest.integration.config.ts
    - package-lock.json
  modified: []

key-decisions:
  - "module=nodenext (not bundler) — enforces .js extensions on relative imports, preventing ERR_MODULE_NOT_FOUND when running dist/ output directly in Node.js"
  - "Two separate Vitest configs — unit tests have no Docker dependency and must stay fast; integration tests require 120s timeout for Docker cold pull"
  - "Pinned exact versions (not ranges) in package.json — acceptable supply chain posture for dev scaffold; package-lock.json provides hash verification"

patterns-established:
  - "Pattern 1: nodenext ESM — all relative imports must use .js extension in source (TypeScript rewrites at compile time)"
  - "Pattern 2: Test separation — vitest.config.ts for unit only; vitest.integration.config.ts for Docker-backed tests"

requirements-completed: [ARCH-01, ARCH-02, TEST-02, TEST-03]

# Metrics
duration: 1min
completed: 2026-04-20
---

# Phase 01 Plan 01: Project Configuration Layer Summary

**ESM TypeScript scaffold with nodenext module resolution, pinned deps (typescript 6.0.3, vitest 4.1.4, tsx 4.21.0), and separated unit/integration Vitest configs**

## Performance

- **Duration:** 1 min
- **Started:** 2026-04-20T20:13:58Z
- **Completed:** 2026-04-20T20:15:05Z
- **Tasks:** 2
- **Files modified:** 5

## Accomplishments
- package.json with type=module, bin entry for CLI, all 6 scripts, and all pinned exact dependencies
- tsconfig.json with nodenext module resolution, strict=true, verbatimModuleSyntax=true — enforces .js import extensions required by Node.js ESM runtime
- Two Vitest configs with strict test scope separation: unit tests (no Docker, 10s timeout) vs integration tests (Docker, 120s timeout, 30s hookTimeout)
- npm install complete — 53 packages, 0 vulnerabilities

## Task Commits

Each task was committed atomically:

1. **Task 1: Write package.json and tsconfig.json** - `d0e1bb6` (chore)
2. **Task 2: Write Vitest configs and install dependencies** - `570e25e` (chore)

**Plan metadata:** (docs commit — see below)

## Files Created/Modified
- `package.json` - ESM package config with bin entry, 6 scripts, pinned devDependencies and dependencies
- `tsconfig.json` - TypeScript compiler config for Node.js ESM CLI (nodenext, strict, verbatimModuleSyntax)
- `vitest.config.ts` - Unit test runner (tests/unit/**/*.test.ts, src/**/*.test.ts; no integration; testTimeout=10000)
- `vitest.integration.config.ts` - Integration test runner (tests/integration/**/*.test.ts; testTimeout=120000; hookTimeout=30000)
- `package-lock.json` - npm lockfile generated by npm install (53 packages)

## Decisions Made
- Used `module=nodenext` (not `bundler` as mentioned in D-01 CONTEXT.md) — nodenext enforces .js extensions on relative imports preventing ERR_MODULE_NOT_FOUND when running compiled dist/ output directly in Node.js. Bundler is only correct for Vite/webpack pipelines.
- Strict separation of unit and integration Vitest configs as two separate files rather than one config with env flags — config-level separation is explicit and avoids accidental cross-contamination.

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered

None.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

- Project tooling foundation complete. All subsequent plans in Phase 1 (01-02 through 01-04) can now install types, run the TypeScript compiler, and use `vitest run` for unit tests.
- node_modules is populated; `npm test` is ready to run (will report "no tests found" until test files are created).
- Integration test infrastructure (docker-compose.yml, scripts/test-integration.sh) is planned in 01-02 or later plans.

---
*Phase: 01-foundation*
*Completed: 2026-04-20*
